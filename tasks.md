- Featured Slider trước khi tách Day/Highland Tours hơi trùng lặp — Slider đã show tour nổi bật (gộp cả dayTours + highlandTours), sau đó lại liệt kê riêng 2 khối Day Tours và Highland Tours ngay bên dưới → khách có thể thấy tour lặp lại 2-3 lần liên tiếp khi cuộn. Cân nhắc: bỏ Slider riêng và dùng luôn 2 khối Day/Highland Tours làm phần "hero sản phẩm", hoặc gộp Slider làm 1 phần trong Hero (đây là đề xuất thiết kế/UX cho phiên bản sau khi có thêm nhiều tour thực tế).
- thay cac file i18n tu t-about-page.ts thanh suffix nhu about-page.i18n.ts, làm tương tự với cà files còn lại và tour-page.ts (sửa từ tours-page → khớp module tour-page)
- breadcum cua tours ko đúng, ví dụ detail day tours hoac highland tours khi vao breadcum đều hiển thị: "Trang chủ / tour trong ngày / Detail" => "Trang chủ / tour trong ngày / chèo thuyền" ví dụ vậy thay vì details
- kiểm tra lại các chổ nào vẫn hardcoded cho i18n mà có thể thay thế thì tách thành file riêng hết, Điểm đáng chú ý phát sinh khi soi lại đoạn này: chỗ breadcrumb này cũng đang dính đúng lỗi i18n mình nói ở câu trả lời trước — {$locale === 'vn' ? 'Trang chủ' : 'Home'}
